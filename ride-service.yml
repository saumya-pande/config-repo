server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ridedb?createDatabaseIfNotExist=true
    username: root
    password: 3141@MySQL
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    publisher-confirm-type: correlated
    publisher-returns: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
